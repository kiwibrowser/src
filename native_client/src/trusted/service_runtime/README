This directory contains code for native client's service runtime
 which provides a limited set of operating system style services
to native client modules.

The following scons targets are used for testing this code:

    * run_service_runtime_tests
    * simple_tests
