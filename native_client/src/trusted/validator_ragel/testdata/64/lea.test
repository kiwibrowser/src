@hex:
  # Show that just computing an address, using LEA, doesn't cause an error.
  8d 0c c1
@dis:
     0:	8d 0c c1                                     	lea    (%rcx,%rax,8),%ecx
@rdfa_output:
  return code: 0
@spec:
  SAFE
