This directory contains new ragel-based validator.

Currently it's not used in productions can only be tested using
scripts from this directory.

The plan is to incorporate tests from other directories to make
sure it's production-quality before we will add it to sel_ldr and
Chrome.
