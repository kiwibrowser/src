
This directory contains examples of small programs that should be rejected
by the Native Client validator, generally because they attempt to use system
call instructions. They should build okay on MacOS and Linux.
